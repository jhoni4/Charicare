@model Charicare2.Models.AppViewModels.StripeFormViewModel

@{
    ViewData["Title"] = "Donor's  card information";
}


<div class="row FormContainer">
    <div class="col-md-6 panel panel-success">
        <div class="panel-heading" style="background-color:black; color:chartreuse;">
            <h3>@ViewData["Title"]</h3>
        </div>
        <div class="CustomerForm">
                <hr />
                <br />
                <div class="container" style="color:darkblue;">
                    <form action="Charge" method="POST" id="payment-form">
                            <span class="payment-errors" style="background-color:pink; font-size:1.2em;"></span>

                            <div class="form-row">
                                <label>
                                    <span>Card Number</span>
                                    <input type="text" size="20" data-stripe="number">
                                </label>
                            </div>

                            <div class="form-row">
                                <label>
                                    <span>Expiration (MM/YY)</span>
                                    <input type="text" size="2" data-stripe="exp_month">
                                </label>
                                <span> / </span>
                                <input type="text" size="4" data-stripe="exp_year">
                            </div>

                            <div class="form-row">
                                <label>
                                    <span>CVC</span>
                                    <input type="text" size="4" data-stripe="cvc">
                                </label>
                            </div>


                            <input type="submit" class="submit" value="Submit Payment">
                        </form>
                </div>
            </div>
        </div>
    </div>
    @section Scripts {
        @{ await Html.RenderPartialAsync("_ValidationScriptsPartial"); }
    }
